version: '3.8'

services:
  meter-monitor:
    build: .
    container_name: meter-monitor
    restart: unless-stopped
    ports:
      - "3004:3004"
    environment:
      - PORT=3004
      - DASHBOARD_LAN_URL=http://localhost:3004/ui/index.html
